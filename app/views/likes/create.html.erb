<% if current_user.liked?(post) %>
  <%= link_to "Unlike", like_path(post.likes.find_by(user: current_user)), method: :delete, remote: true %>
<% else %>
  <%= link_to "Like", likes_path(post_id: post.id), method: :post, remote: true %>
<% end %>
