 <% comments.each do |comment|%>
 <p>
    <b> <%=comments.first.user&.email%> </b>
    
    <%= comment.comments%>
  </p>
  <%= link_to "Delete Comment",[comment.post , comment] , data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %>

<% end %>
    

